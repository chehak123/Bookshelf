<%- include('partials/header'); -%>
<h1>Home</h1>
<p><%= homeStartingContent %></p> 



<% posts.forEach(function(post){ %>
   <% const truncated = post.body.substring(0,100) +" ..." ;  %>
    <h1><%= post.title %></h1>
    <p><%= truncated %>
      <a href="/posts/<%=post.title %>">  Read More</a>
    </p>
<% }) %>
    

<%- include('partials/footer'); -%>